@model DetailsClientDTO
@inject UserManager<BaseUser> userManager
@{
    ViewData["Title"] = "تفاصيل العميل";
    var user = await userManager.GetUserAsync(User);
    bool isFinancialUser = await userManager.IsInRoleAsync(user, Role.Financial);
    bool isGeneralManager = await userManager.IsInRoleAsync(user, Role.GeneralManager);

    var i = 0;
    int newFileId = 0;
}

<!-- bootstrab table -->
<form class="row" asp-area="" asp-controller="client" asp-action="SavedFileAboutClient" enctype="multipart/form-data" >
    <input type="hidden" asp-for="ClientId" />
    <div class="row">
        <h3 class="mb-3">المعلومات الرئيسية</h3>
        <div class="row client-info card p-3">
            <div class="row client-info-container">
                <div class="client-info-box col-md-4">
                   <h5 class="m-0 info-header">اسم العميل</h5>
                   <h5 class="m-0 info">@Model.ClientName </h5>
                </div>
                <div class="client-info-box col-md-4">
                    <h5 class="m-0 info-header">رقم الهاتف</h5>
                    <h5 class="m-0 info">@Model.PhoneNumber</h5>
                </div>
                <div class="client-info-box col-md-4">
                    <h5 class="m-0 info-header">العنوان</h5>
                    <h5 class="m-0 info">@Model.Address</h5>
                </div>
            </div>

            <div class="row client-info-container mt-2">
                <div class="client-info-box col-md-4">
                    <h5 class="m-0 info-header">تفاصيل احتياج العميل</h5>
                    <h5 class="m-0 info">@Model.CustomerNeeded</h5>
                </div>
                <div class="client-info-box col-md-4">
                    <h5 class="m-0 info-header">تم انشاء الملف بواسطه</h5>
                    <h5 class="m-0 info">@Model.CreatedBy</h5>
                </div>
                <div class="client-info-box col-md-4">
                    <h5 class="m-0 info-header">تم التعامل مع الملف بواسطه</h5>
                    <h5 class="m-0 info">@Model.HandledBy</h5>
                </div>
            </div>


            <div class="client-info-container align-items-center row mt-2">
                <div class="client-info-box client-notes col-sm-12 col-md-9 p-0">
                    <strong class="info-label">ملاحظات</strong>
                    <input class="client-notes-input text-end fs-5 pe-2" asp-for="Notes" value="@Model.Notes" />
                </div>

               
                <div class="d-flex justify-content-center align-self-end client-control col-sm-12 col-lg-3 mt-xs-2 mt-sm-2">
                    @if(!string.IsNullOrEmpty(Model.BillsFileOld))
                    {

                        <div class="d-flex justify-content-center align-items-end">
                            <button data-id="@Model.ClientId" class="btn btn-primary fs-6 m-0">
                                    عرض الفاتورة الحالية
                            </button>
                        </div>
                    }
                    @if (isFinancialUser || isGeneralManager)
                    {
                        <div class="btn-absoulte-for-upload d-flex justify-content-center align-items-end">
                            <input type="file" class="hidden-file-input" />
                            <button type="button" asp-for="BillsFileNew" class="btn btn-primary fs-6 m-0">
                                رفع فاتورة
                            </button>
                        </div>
                    }
                </div>
            </div>

        </div>

        <h3 class="mt-5 mb-1"> ملفات اخرى</h3>

        <div class="row card mt-4 mb-4">

            <table class="table table-bordered table-hover">
                <thead>
                    <tr class="text-dark">
                        <th scope="col">#</th>
                        <th scope="col">الملف</th>
                        <th scope="col">وصف الملف</th>
                        <th scope="col">اضافة</th>
                        <th scope="col">مسح</th>
                    </tr>
                </thead>
                <tbody id="appendNewFile">
                    @foreach(var item in Model.OldOtherFiles)
                    {
                        <tr>
                            <th scope="row">@(++i)</th>
                            <td>
                                <div class="d-flex flex-column justify-content-center align-items-center">
                                    <button type="button" id="downloadFile" data-downloadfilebyname="@item.FileOld" class="btn btn-primary fs-4">
                                            <i class="fas fa-download"></i>
                                    </button>
                                </div>
                            </td>
                            <td>@item.Description</td>
                            <td></td>
                            <td></td>
                        </tr>
                    
                    }

                    <tr class="">
                        <th scope="row">@(++i)</th>
                        <td>
                            <input class="test" id="hasFile-@i" asp-for="NewOtherFiles[newFileId].NewFile" type="file">
                        </td>
                        <td>
                            <input id="hasDescription-@i" asp-for="NewOtherFiles[newFileId].Description" type="text">
                        </td>
                        <td class="d-flex justify-content-center">
                            <button id="remove-@i" type="button" class="add-new-otherFile table-add-btn">
                                <i class="fas fa-plus fs-6"></i>
                            </button>
                        </td>
                        <td></td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="row justify-content-center">
            <div class="d-flex col justify-content-center">
                <button class="btn btn-primary">حفظ التعديلات</button>
            </div>
            @if (isFinancialUser){
                <div class="d-flex col justify-content-center">
                    <a class="btn btn-secondary">انهاء الملف</a>
                </div>
            }
            else{
                <div class="d-flex col justify-content-center">
                    <a id="transformToFinancial" data-getclientid="@Model.ClientId" class="btn btn-secondary">تحويل الي المالية</a>
                </div>
            }

            <div class="d-flex col justify-content-center">
                <a asp-area="" asp-controller="client" asp-action="index" class="btn btn-secondary">ارجاع الي القائمة</a>
            </div>
        </div>
    </div>
</form>

@section Scripts{

    <script>
    
        var currentIndex = @(++i);
        var ids = @i;

        $(document).ready(function () {
            $(document).on("click", "#selectDepartment", function () {
                var subCat = this;
                $.ajax({
                    url: "GetUsersByDepartmentId?departmentId=" + $(subCat).val(),
                    type: "GET",
                    contentType: "application/json; charset=utf-8",
                    datatype: JSON,
                    success: function (result) {
                        debugger;
                        var users = '<option selected disabled value="0">اختار الموظف</option>';
                        $(result).each(function () {
                            users = users + '<option value="' + this.id + '">' + this.value + '</option>'
                        });
                        var subCateList = $("#selectUsers");
                        subCateList.empty();
                        subCateList.append()
                        subCateList.append(users);
                    },
                    error: function (data) {
                        return Swal.fire(
                            "حدث خطأ اثناء اختيار القسم"
                        );
                    }
                });
            });
        });

        $(document).on("click", ".add-new-otherFile", function () {

            let idOfFile = `hasFile-${ids}`;
            let idOfDescriptions = `hasDescription-${ids}`;

            let file = document.getElementById(`${idOfFile}`).value;

            let descriptions = document.getElementById(`${idOfDescriptions}`).value;

            console.log(file)
            console.log(descriptions)

            if (file!== ''  && descriptions !== '') {

                let removeId = "remove-" + ids;


                let removeButton = document.getElementById(removeId);

                removeButton.removeChild(removeButton.lastElementChild);

                removeButton.remove();

                $("#appendNewFile").append(`
                    <tr class="table-primary">
                        <th scope="row">${currentIndex}</th>
                        <td>
                            <input type="file" id="hasFile-${ids}" name="NewOtherFiles[${ids}].NewFile" >
                        </td>
                        <td>
                            <input id="hasDescription-${ids}" name="NewOtherFiles[${ids}].Description" type="text">
                        </td>
                        <td>
                            <button  id="remove-${ids}" type="button" class="add-new-otherFile btn btn-primary table-add-btn">
                                <i class="fas fa-plus-circle fs-6"></i>
                            </button>
                        </td>
                        <td>
                            <button id="delete-${ids}" style="background-color:darkred"  type="button" class="add-remove-row btn btn-primary table-add-btn">
                                <i class="fas fa-minus-circle fs-6"></i>
                            </button>
                        </td>
                    </tr>
         
            `);
            
            currentIndex+=1;
            ids+=1;

            }
            else{
                Swal.fire(
                    'يجب اختيار الملف و كتابة تفاصيل له.'
                )
            }


        });


        $(document).on("click", "#downloadFile", function(){
            var downloadFile = $(this).data("downloadfilebyname");
            console.log(downloadFile);

            const inputEle = document.querySelector('[data-downloadFile="download"]')
            window.open(document.location.origin + "/client/DownloadFile?fileName=" + downloadFile, '_blank');
        });


        $(document).on("click", "#transformToFinancial", function(){
            var clientId = $(this).data("getclientid");
            $.ajax({
                url: '/Client/TransformFileToFinancial?clientId='+clientId,
                type: "POST",
                contentType: 'application/x-www-form-urlencoded',
                data: clientId,
                success: (data) => {
                   
                },
                error: function (eventFromParam) {
                    Swal.fire(
                    "حدث خطأ أثناء التحويل للماليه حاول مرة اخرى"
                    )
                }
            })

        });

</script>
}

